<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="./css/styles.css" rel="stylesheet">
    <title>Online Voting Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %>
            <h1 class="text-2xl"><%= electionTitle %></h1>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                        <li> <%= messages.error[i] %></li>
                <% } %>
              <% } %>
            <h3 class="text-lg py-2">Share this URL with the voters <a class="text-green-600" href="/vote/election/<%= electionId %>">https://online-voting-sj3r.onrender.com/vote/election/<%= electionId %></a></h3> 
            <div>
                <a href="/elections/manage/<%= electionId %>/manageVoters" class="text-green-600 text-lg py-4">Manage Voters</a>
                <br>
                <a href="/elections/manage/<%= electionId %>/manageQuestions" class="text-green-600 text-lg py-4">Manage Questions</a>
            </div>
        </div>
    </div>
</body>
</html>